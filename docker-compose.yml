version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - PORT=8080
      - DATABASE_PATH=/data/tracker.db
      - GIN_MODE=release
    restart: unless-stopped
